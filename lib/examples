1. Create trajectory in visualizer application. Save it somewhere (let's say /home/usr/1.track)
2. Run command:
```
cat ~/1.track | ./data_generator/generator/build/mlm_data_generator --acceleration-time 1.0 --accelerometer-measurement-period 0.1 --gps-measurement-period 2.0 --accelerometer-noise 0.001 --gps-noise 0.3 --output /home/usr/2.track
```
3. Run command:
```
cat ~/2.track | ./filter/build/mlm_filter > ~/3.track
```
4. Load files 2.track and 3.track into visualizer
